interface:
  display_name: "LabKit Test Planner"
  short_description: "Select focused LabKit validation evidence"
  default_prompt: "Use $labkit-test-planner to select and run the smallest source-owned evidence for this change."
