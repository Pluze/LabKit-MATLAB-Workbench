function debt = labkitToolboxDebt()
%LABKITTOOLBOXDEBT Declare temporary, tested MathWorks product dependencies.
% Expected callers are project compatibility guardrails. Output is a struct
% array with id, source, symbol, product, owner, fallbackTest, parityTest, and
% replacement fields. Each entry must also have an active record with the same id in
% .agents/migration_guide.md. An empty array means no accepted toolbox debt.

    debt = struct( ...
        'id', {}, ...
        'source', {}, ...
        'symbol', {}, ...
        'product', {}, ...
        'owner', {}, ...
        'fallbackTest', {}, ...
        'parityTest', {}, ...
        'replacement', {});
end
