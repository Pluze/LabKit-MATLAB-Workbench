# Tests

Run the default MATLAB suite from the repository root:

```bash
scripts/run_matlab_tests.sh
```

Focused profiles and suite/test filters are documented in `docs/testing.md`. Keep this file as a short pointer so test guidance does not drift across two locations.
