% Expected caller: Figure Studio UI/action helpers. Inputs are none. Output is
% the ordered figure-style modes shown in the app. Side effects are none.
function names = presetNames()
    names = ["LabKit figure", "FIG default", "Custom imported"];
end
