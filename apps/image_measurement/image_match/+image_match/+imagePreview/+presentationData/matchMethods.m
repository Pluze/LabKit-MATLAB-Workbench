% Expected caller: Image Match layout, presenter, and tests. Output is the
% single app-owned list of legal reference-match method labels.
function values = matchMethods()
    values = {'Balanced', 'White balance', 'Tone only', ...
        'Protected tone', 'Lab style', 'Histogram'};
end
