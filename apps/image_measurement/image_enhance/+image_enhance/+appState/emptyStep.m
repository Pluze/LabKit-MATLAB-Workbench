% Expected caller: labkit_ImageEnhance_app and image_enhance package tests.
% Output is one enhancement-history step. kind names match the app dropdown;
% amount and secondary are numeric controls whose meaning depends on kind.
function step = emptyStep()

    step = struct( ...
        'kind', "", ...
        'amount', 0, ...
        'secondary', 0, ...
        'referenceIndex', 0, ...
        'label', "");
end
