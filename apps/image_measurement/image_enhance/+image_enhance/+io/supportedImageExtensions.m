% Expected caller: image_enhance IO helpers and tests. Output is the supported
% lowercase extension list for source images.
function extensions = supportedImageExtensions()

    extensions = [".png", ".jpg", ".jpeg", ".tif", ".tiff", ".bmp"];
end
