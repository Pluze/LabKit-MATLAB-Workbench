% Transitional render hook for Video Marker. Expected caller is
% labkit.ui.runtime.run after action dispatch. Rendering is action-driven
% because the app owns live video reader and point-editor handles.
function updateWorkbenchFromState(~, ~, ~)
end
