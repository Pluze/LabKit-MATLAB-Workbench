% Expected callers: Focus Stack layout, settings mapper, and tests. Output is
% the single ordered source of truth for user-visible fusion preset labels.
function items = fusionPresetItems()
    items = ["Balanced", "Crisp details", ...
        "Smooth transitions", "Noisy images"];
end
