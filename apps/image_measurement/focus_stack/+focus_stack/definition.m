% App-owned runtime definition for labkit_FocusStack_app. Expected caller:
% the public app entrypoint. Output is a declarative LabKit app definition;
% side effects are none.
function def = definition()
    project = struct( ...
        "Version", 1, ...
        "Create", @focus_stack.appLifecycle.createProject, ...
        "Validate", @focus_stack.appLifecycle.validateProject, ...
        "Migrations", {{}});
    def = labkit.ui.runtime.define( ...
        "Id", "focus_stack", ...
        "Title", "Microscope Focus Stack Fusion", ...
        "Project", project, ...
        "CreateSession", @focus_stack.appLifecycle.createSession, ...
        "Layout", @focus_stack.userInterface.buildWorkbenchLayout, ...
        "Actions", focus_stack.definitionActions(), ...
        "Present", @focus_stack.userInterface.presentWorkbench, ...
        "Renderers", struct("focusImage", ...
            @focus_stack.userInterface.renderFocusImage), ...
        "Start", @focus_stack.startup, ...
        "DebugSample", @focus_stack.debug.writeSamplePack);
end
