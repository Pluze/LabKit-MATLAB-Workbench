% App-owned state factory for EIS Overlay. Expected caller is the LabKit app
% runtime. Output is the mutable app state struct used by actions/render.
% Side effects are none.
function state = initial()
    state = struct();
    state.items = struct([]);
    state.axisItems = { ...
        'Freq (Hz)', ...
        'log10(Freq)', ...
        'Time (s)', ...
        'Point #', ...
        'Zreal (ohm)', ...
        'Zimag (ohm)', ...
        '-Zimag (ohm)', ...
        'Zmod (ohm)', ...
        'Zphz (deg)', ...
        'Idc (A)', ...
        'Vdc (V)'};
end
