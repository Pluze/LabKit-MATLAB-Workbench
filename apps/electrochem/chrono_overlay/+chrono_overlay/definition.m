% App-owned runtime definition for labkit_ChronoOverlay_app. Expected caller:
% the public app entrypoint. Output is a declarative LabKit app definition;
% side effects are none.
function def = definition()
    def = labkit.ui.app.define( ...
        "Id", "chrono_overlay", ...
        "Title", "Gamry Multi-DTA Plot Export GUI", ...
        "InitialState", @chrono_overlay.state.initial, ...
        "Spec", @chrono_overlay.ui.buildSpec, ...
        "Actions", chrono_overlay.actions.table(), ...
        "Render", @chrono_overlay.view.render, ...
        "Startup", "startup");
end
