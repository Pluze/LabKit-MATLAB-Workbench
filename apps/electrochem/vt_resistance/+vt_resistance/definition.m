% App-owned runtime definition for labkit_VTResistance_app. Expected caller:
% the public app entrypoint. Output is a declarative LabKit app definition;
% side effects are none.
function def = definition()
    project = struct( ...
        "Version", 1, ...
        "Create", @vt_resistance.appLifecycle.createProject, ...
        "Validate", @vt_resistance.appLifecycle.validateProject, ...
        "Migrations", {{}});
    def = labkit.ui.runtime.define( ...
        "Id", "vt_resistance", ...
        "Title", "Gamry VT Steady Resistance GUI", ...
        "Project", project, ...
        "CreateSession", @vt_resistance.appLifecycle.createSession, ...
        "Layout", @vt_resistance.userInterface.buildWorkbenchLayout, ...
        "Actions", vt_resistance.definitionActions(), ...
        "Present", @vt_resistance.userInterface.presentWorkbench, ...
        "Renderers", struct("resistanceAxis", ...
            @vt_resistance.userInterface.renderResistanceAxis), ...
        "DebugSample", @vt_resistance.debug.writeSamplePack);
end
