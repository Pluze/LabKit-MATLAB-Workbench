% App product factory; returns the complete runtime definition without side effects.
function def = definition()
%DEFINITION Return the T-Test Wizard runtime product contract.
%
% Expected caller: labkit_TTestWizard_app. The output declares the product
% version, project/session factories, semantic layout, actions, presenter,
% plot renderer, and synthetic debug sample. Side effects are none.

    def = labkit.ui.runtime.define( ...
        "Command", "labkit_TTestWizard_app", ...
        "Id", "ttest_wizard", ...
        "Title", "T-Test Wizard", ...
        "DisplayName", "T-Test Wizard", ...
        "Family", "Statistics", ...
        "AppVersion", "1.0.1", ...
        "Updated", "2026-07-19", ...
        "Requirements", labkit.contract.requirements("ui", ">=7 <8"), ...
        "Project", ttest_wizard.projectSpec(), ...
        "CreateSession", @ttest_wizard.createSession, ...
        "Layout", @ttest_wizard.userInterface.buildWorkbenchLayout, ...
        "Actions", ttest_wizard.definitionActions(), ...
        "Present", @ttest_wizard.userInterface.presentWorkbench, ...
        "Renderers", struct("resultPreview", ...
            @ttest_wizard.userInterface.drawResultPreview), ...
        "DebugSample", @ttest_wizard.debug.writeSamplePack);
end
