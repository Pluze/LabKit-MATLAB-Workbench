% Transitional renderer for RHS Preview. Expected caller is labkit.ui.app.run
% after action dispatch. Rendering remains action-driven while this app moves
% from the package-root runner toward workflow-first UI updates.
function render(~, ~, ~)
end
