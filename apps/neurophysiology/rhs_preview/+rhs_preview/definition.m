% App-owned runtime definition for labkit_RHSPreview_app.
% Expected caller: the public app entrypoint. Output is a declarative LabKit
% app definition; side effects are none.
function def = definition()
    def = labkit.ui.app.define( ...
        "Id", "rhs_preview", ...
        "Title", "RHS Preview", ...
        "InitialState", @rhs_preview.appLifecycle.createInitialState, ...
        "Spec", @rhs_preview.userInterface.buildWorkbenchSpec, ...
        "Actions", rhs_preview.definitionActions(), ...
        "Render", @rhs_preview.userInterface.updateWorkbenchFromState, ...
        "Startup", "startup");
end
